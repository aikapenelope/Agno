# NEXUS Cerebro - Dependencies
# Based on official Agno optional dependency groups from pyproject.toml

# Core: Agno with AgentOS runtime (includes FastAPI, uvicorn, sqlalchemy, tracing)
agno[os]

# Models: Groq + MiniMax (via OpenAI-compatible API)
agno[groq]
agno[openai]

# Tools: Free (no API key) - agno extras + extra pip deps
agno[arxiv]
agno[ddg]
agno[newspaper]
agno[youtube]
pypdf              # required by ArxivTools to read downloaded PDFs
praw               # required by RedditTools
yfinance           # required by YFinanceTools
wikipedia          # required by WikipediaTools
newspaper4k[nlp]   # full NLP features for Newspaper4kTools

# Tools: Require API keys (install now, configure keys later)
agno[exa]
agno[github]
agno[slack]
agno[tavily]
agno[todoist]

# Tools: Additional integrations (guarded by env vars at runtime)
tweepy             # required by XTools (X/Twitter)
firecrawl-py       # required by FirecrawlTools (web scraping)
spider-client      # required by SpiderTools (web crawling)
browserbase        # required by BrowserbaseTools (browser automation)
google-genai       # required by NanoBananaTools (image generation)
lumaai             # required by LumaLabTools (video generation)

# Tools: MCP protocol
agno[mcp]

# Storage: SQLite backend
agno[sqlite]

# Knowledge: LanceDB vector store + Voyage AI embeddings
lancedb
pylance
tantivy
voyageai
